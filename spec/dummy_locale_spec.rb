require 'spec_helper'

describe DummyLocale do
  it 'has a version number' do
    expect(DummyLocale::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
